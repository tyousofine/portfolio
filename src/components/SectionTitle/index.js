import React from 'react'

import './styles.scss'

export default function SectionTitle({ sectionTitle }) {
    return (

        <div className="sectionTitle">
            <h4>{sectionTitle}</h4>
        </div>

    )
}